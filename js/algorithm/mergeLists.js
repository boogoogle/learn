/**
 * @param {ListNode[]} lists
 * @return {ListNode}
 */
const lists = [[1, 4, 5], [1, 3, 4], [2, 6]]
// output [1,1,2,3,4,4,5,6]
var mergeKLists = function (lists) {

  let result = lists[0]
  let currentListIndex = 1
  for(currentListIndex; currentListIndex < lists.lenght; currentListIndex++){
    let currentList = lists[currentListIndex]
    
  }

};

mergeKLists(lists)